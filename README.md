# Algorithmic-Structural-Causal-Influence
Metrics to quantify and assess algorithmic fairness in dynamic and longitudinal settings, particularly when there are feedback loops that perpetuate inequality.

FileDescriptionKey Functions & Variablesconfig.pyCentral configuration for the simulation.Constants and hyperparameters (e.g., $N, T, \tau, G$, VAR matrix, harm weights).metrics.pyCore evaluation and fairness logic.Harm functions, dSCI, eSCI path, DP/EO gaps, Liu curve, CVaR, tail analysis.simulation.pyEnvironment and agent logic.initialize_population, make_decisions, var_feedback_transition, update_structural_attribute, counterfactual_intervention, run_simulation.monte_carlo.pyStatistical robustness and aggregation.run_monte_carlo — executes $N$ independent seeds and aggregates metrics.plots.pyVisualization and reporting tools.All plot_* functions and print_results_table.main.pyApplication entry point.Execute python main.py to reproduce the full analysis.
